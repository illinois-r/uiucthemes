title: UIUC Illinois Beamer Theme
author: James Joseph Balamuta
version: 1.0.0
quarto-required: ">=1.3.0"
contributes:
  formats:
    beamer:
      # Classic "Illinois" look: AnnArbor structure recolored with the
      # official University of Illinois Urbana-Champaign blue and orange.
      theme: AnnArbor
      colortheme: wolverine
      navigation: empty
      # This classic theme uses stock (Type1-era) beamer themes; pdflatex is
      # the natural, fast engine and avoids Unicode-font setup.
      pdf-engine: pdflatex
      include-in-header:
        - uiuc-illinois.tex
      # Ship the Illinois logo so the title-slide \includegraphics can find it.
      format-resources:
        - ilogo.pdf
